/* Placeholder, might need more files */

function initJS(data)
{
    return JSON.parse(data)
}

initJS().then (function(data) {
    
    var margin = {top: 30, right: 50, bottom: 30, left: 50};
    var svgWidth = 600;
    var svgHeight = 270;
})

